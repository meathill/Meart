<!DOCTYPE html>
<html lang="zh-cmn-Hans">
<head>
  <meta charset="UTF-8">
  <title>{{siteTitle}}</title>
  <meta name="keywords" content="{{siteTags}}">
  <meta name="description" content="{{siteDesc}}">
  <meta name="robots" content="index,follow">
  {{#if siteIcon}}
  <link rel="shortcut icon" type="image/png" href="./img/fav.png">
  {{/if}}
  <link rel="stylesheet" href="http://cdn.staticfile.org/twitter-bootstrap/4.0.0-alpha.5/css/bootstrap.min.css">
  <link rel="stylesheet" href="css/screen.css">
</head>
<body>
<div class="container">
  <h1>{{siteTitle}}</h1>
  <hr>
  <ul class="row list">
    {{#each articles}}
      {{#if id}}
    <li class="col-sm-3 col-xs-12">
      <a class="card" href="./{{url}}.html">
        {{#if thumbnail}}
          <img src="{{thumbnail}}" class="img-fluid card-img-top" alt="{{title}}">
        {{/if}}
        <div class="card-block">
          <h4 class="card-title">{{title}}</h4>
          <p class="card-text">{{description}}</p>
          <p class="card-text">
            <small class="text-muted">创建于：<time datetime="{{toDate createTime}}">{{toCalendar createTime}}</time></small>
            <small class="text-muted">最新：<time datetime="{{toDate lastModifiedTime}}">{{toCalendar lastModifiedTime}}</time></small>
          </p>
        </div>
      </a>
    </li>
      {{/if}}
    {{/each}}
  </ul>
</div>

</body>
</html>