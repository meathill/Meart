<!DOCTYPE html>
<html lang="zh-cmn-Hans">
<head>
  <meta charset="UTF-8">
  <title>{{title}} | {{siteTitle}}</title>
  <meta name="keywords" content="{{tags}}">
  <meta name="description" content="{{description}}">
  <meta name="robots" content="index,follow">
  {{#if siteIcon}}
    <link rel="shortcut icon" type="image/png" href="./img/fav.png">
  {{/if}}
  <link rel="stylesheet" href="http://cdn.staticfile.org/twitter-bootstrap/4.0.0-alpha.5/css/bootstrap.min.css">
  <link rel="stylesheet" href="css/screen.css">
</head>
<body>
<div class="container">
  <h1>{{title}}</h1>
  <p class="lead">{{description}}</p>
  <p>
    <small>创建时间：<time datetime="{{toDate createTime}}">{{toCalendar createTime}}</time></small> |
    <small>更新时间：<time datetime="{{toDate lastModifiedTime}}">{{toCalendar lastModifiedTime}}</time></small>
  </p>
  <hr>
  <ul class="row list">
    {{#each album}}
      <li class="col-sm-3 col-xs-12">
        <div class="card">
          <img src="{{src}}" alt="{{title}}" class="img-fluid card-img-top">
          <div class="card-block">
            <h4 class="card-title">{{title}}</h4>
            <p class="card-text">{{description}}</p>
          </div>
        </div>
      </li>
    {{/each}}
  </ul>
</div>
</body>
</html>